import 'package:flutter/material.dart';
import 'styles.dart';

abstract class NavigationIcons {
  static const menu = Icon(Icons.menu);
  static const dashboard = Icon(Icons.grid_view);
  static const topicSelection = Icon(Icons.chat_bubble_outline);
  static const mockTest = Icon(Icons.description_outlined);
  static const myProgress = Icon(Icons.bar_chart);
} 